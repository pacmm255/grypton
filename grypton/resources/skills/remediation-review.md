# Remediation notes

For a validated finding, describe the violated security invariant and the
smallest control that restores it. Include a regression check built from the
saved positive/control pair. Keep remediation separate from evidence and never
claim a fix was deployed or tested unless an artifact proves it.
