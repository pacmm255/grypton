# Scope control

Read `scope-rules.md` before network work. Structured network tools enforce the
recorded host rules; use them whenever possible. Never follow redirects to an
unlisted host, expand a base domain without a wildcard scope rule, or treat a
discovered third-party hostname as permission to test it. When a useful clue is
out of scope, record the clue without contacting that host and continue inside
scope.
